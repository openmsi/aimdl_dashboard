[project]
name = "aimdl-dashboard-api"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "girder-client>=3.2",
    "httpx>=0.27",
]

[project.optional-dependencies]
dev = ["pytest", "ruff"]
